# Development and test dependencies. Install on top of the runtime pins:
#
#     python3.12 -m venv .venv && . .venv/bin/activate
#     pip install -r requirements.txt -r requirements-dev.txt
#     python3 -m pytest
#
# Python 3.12, matching the Dockerfile and the weekly workflow. pandas 2.1.4
# and numpy 1.26.4 publish no wheels for 3.13, so a 3.13 interpreter silently
# forces a different pandas than the one production runs.
-r requirements.txt

pytest==8.3.4

# Reads the vendored Space Grotesk faces in static/fonts/ so
# tests/test_nav_panel_width.py can measure real chart names against the chart
# menu's width, rather than guessing at an average glyph. Test-only: the site
# loads its fonts from Google Fonts and never imports this.
fonttools==4.55.3
